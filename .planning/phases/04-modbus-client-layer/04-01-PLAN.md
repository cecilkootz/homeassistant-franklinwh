---
phase: 04-modbus-client-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/franklin_wh/manifest.json
autonomous: true
requirements:
  - MDATA-01
  - MDATA-02
  - MDATA-03
  - MDATA-04

must_haves:
  truths:
    - "pysunspec2==1.3.3 appears in manifest.json requirements array"
    - "HA will pip-install pysunspec2 (PyPI distribution name, not sunspec2) when the integration loads"
  artifacts:
    - path: "custom_components/franklin_wh/manifest.json"
      provides: "Integration manifest with pysunspec2 dependency declared"
      contains: "pysunspec2==1.3.3"
  key_links:
    - from: "custom_components/franklin_wh/manifest.json"
      to: "PyPI pysunspec2"
      via: "requirements array entry"
      pattern: "pysunspec2==1\\.3\\.3"
---

<objective>
Add pysunspec2 to the integration manifest so Home Assistant installs it at integration load time.

Purpose: The SunSpec Modbus client (Plan 02) requires the pysunspec2 library. HA reads manifest.json requirements and installs listed packages via pip. This plan makes that dependency declaration.
Output: manifest.json updated with pysunspec2==1.3.3 in requirements.
</objective>

<execution_context>
@/Users/matt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current manifest.json (full content) -->
From custom_components/franklin_wh/manifest.json:
```json
{
  "domain": "franklin_wh",
  "name": "FranklinWH",
  "codeowners": ["@JoshuaSeidel"],
  "config_flow": true,
  "dependencies": [],
  "documentation": "https://github.com/JoshuaSeidel/homeassistant-franklinwh/wiki",
  "integration_type": "hub",
  "iot_class": "cloud_polling",
  "issue_tracker": "https://github.com/JoshuaSeidel/homeassistant-franklinwh/issues",
  "loggers": ["franklinwh"],
  "requirements": [],
  "version": "1.1.3"
}
```

CRITICAL PyPI naming note: The PyPI distribution name is `pysunspec2` but the Python import name is `sunspec2`. The manifest.json entry MUST use the PyPI distribution name `pysunspec2==1.3.3`. Using `sunspec2==1.3.3` will cause HA's pip install step to fail with "No matching distribution found".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pysunspec2 to manifest.json requirements</name>
  <files>custom_components/franklin_wh/manifest.json</files>
  <action>
Update the `requirements` array in manifest.json to include `"pysunspec2==1.3.3"`.

The current file has `"requirements": []`. Change it to `"requirements": ["pysunspec2==1.3.3"]`.

Do NOT change any other field. Do NOT use `"sunspec2==1.3.3"` — that is the import name, not the PyPI distribution name. The hassfest validator requires keys to be alphabetically sorted after `domain`/`name`; `requirements` already appears in correct position so no reordering needed.

Resulting manifest.json:
```json
{
  "domain": "franklin_wh",
  "name": "FranklinWH",
  "codeowners": ["@JoshuaSeidel"],
  "config_flow": true,
  "dependencies": [],
  "documentation": "https://github.com/JoshuaSeidel/homeassistant-franklinwh/wiki",
  "integration_type": "hub",
  "iot_class": "cloud_polling",
  "issue_tracker": "https://github.com/JoshuaSeidel/homeassistant-franklinwh/issues",
  "loggers": ["franklinwh"],
  "requirements": ["pysunspec2==1.3.3"],
  "version": "1.1.3"
}
```
  </action>
  <verify>
    <automated>python3 -c "import json; m=json.load(open('custom_components/franklin_wh/manifest.json')); assert 'pysunspec2==1.3.3' in m['requirements'], f'missing: {m[\"requirements\"]}'; print('OK:', m['requirements'])"</automated>
  </verify>
  <done>manifest.json requirements array contains exactly `"pysunspec2==1.3.3"`. No other fields changed. File is valid JSON.</done>
</task>

</tasks>

<verification>
Run from repo root:
```bash
python3 -c "import json; m=json.load(open('custom_components/franklin_wh/manifest.json')); assert 'pysunspec2==1.3.3' in m['requirements']; print('manifest OK')"
```
Expected: `manifest OK`
</verification>

<success_criteria>
- manifest.json contains `"pysunspec2==1.3.3"` in requirements
- File is valid JSON (no parse errors)
- No other manifest fields modified
</success_criteria>

<output>
After completion, create `.planning/phases/04-modbus-client-layer/04-01-SUMMARY.md`
</output>
